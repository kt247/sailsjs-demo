/**
 * UserController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {

    // getUsers: async function (req, res) {
    //     return res.send(await User.find({}))
    // },

    // getUserById: async function (req, res) {
    //     let result = await User.findOne({ id: req.params.id })
    //     if (!result) {
    //         throw require('flaverr')({
    //             message: 'User not found',
    //             code: 'ERROR_USER_NOT_FOUND'
    //         })
    //     }
    //     return res.send(result);
    // },

    // postUsers: async function (req, res) {
    //     res.send(await User.create(req.body))
    // },

    // putUsers: async function (req, res) {
    //     res.send(await User.update(await User.findOne({ id: req.params.id }), req.body))
    // },

    // deleteUsers: async function (req, res) {
    //     res.send(await User.destroy(await User.findOne({ id: req.params.id })))
    // },

};

